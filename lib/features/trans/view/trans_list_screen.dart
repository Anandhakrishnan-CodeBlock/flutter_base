import 'package:flutter/material.dart';

class TransListScreen extends StatelessWidget {
  const TransListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
