import 'package:flutter/material.dart';

class CharListScreen extends StatelessWidget {
  const CharListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
